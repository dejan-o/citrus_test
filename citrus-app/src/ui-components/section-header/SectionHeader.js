import React from 'react'

const SectionHeader = ({children, is}) => {
    return (
        <div className="section-header">
            {children}
        </div>
    )
}

export default SectionHeader;
